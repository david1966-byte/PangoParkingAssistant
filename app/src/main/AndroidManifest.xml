<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- זיהוי פעילות (נסיעה/הליכה/עמידה) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- מיקום, כדי לשמור את נקודת החניה -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- שירות קדמה + התראות -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.PangoParkingAssistant"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- שירות קדמה שמפעיל ומחזיק את מעקב הפעילות ברקע -->
        <service
            android:name=".ParkingMonitorService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- מקבל את אירועי המעבר (נסיעה מתחילה/מסתיימת) מ-Activity Recognition -->
        <receiver
            android:name=".ActivityTransitionReceiver"
            android:exported="false" />

        <!-- מפעיל מחדש את השירות אחרי אתחול הטלפון אם המשתמש הפעיל מעקב -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
